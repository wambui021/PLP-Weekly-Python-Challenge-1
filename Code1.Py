# Accept specific user input to create a list of integers
user_numbers = [20, 30, 90, 120]

# Compute the sum of all integers in the list
sum_of_integers = sum(user_numbers)

# Print the result
print(f"The sum of the integers in the list is: {sum_of_integers}")

